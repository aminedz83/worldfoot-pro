name: Test Soccerway Player

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install deps
        run: pip install cloudscraper beautifulsoup4
      - name: Test
        run: python3 scripts/test_squad.py